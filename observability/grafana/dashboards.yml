apiVersion: 1

providers:
    - name: "alentapp"
      folder: 'Alentapp'
      type: file
      allowUiUpdates: true
      updateIntervalSeconds: 10
      options:
        path: /etc/grafana/provisioning/dashboards
